// @flow
function packageErrors(path: string, errorList: string[]){ 
    //should have some errors
    //if no errors do nothing
    //else push them to a zipped package
    //delete log contents
    //return success to the user with a message about the file location

    //if error, throw error return false and alert the user. Log error
}

module.exports.packageErrors = packageErrors;